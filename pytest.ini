[pytest]
testpaths = tests
addopts = -m "not agent and not todo"
markers =
    agent: test drives a live agent session; excluded from the default run, execute with -m agent
    todo: scenario declared without a test yet; must carry an adjacent TODO comment recording why and what unblocks it
